# Off-device training only. NOT installed on the Pi.
# The Pi only needs onnxruntime / opencv (see ../requirements.txt).

ultralytics==8.3.0          # YOLOv8n detector + ONNX export
torch==2.4.1
torchvision==0.19.1
onnx==1.16.2
onnxscript>=0.1.0          # required by torch.onnx.export on PyTorch 2.x
onnxruntime==1.19.2
pillow==10.4.0
numpy==1.26.4
scikit-learn==1.5.2
tqdm==4.66.5
